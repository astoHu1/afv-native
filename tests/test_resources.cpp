#include "afv-native/afv/EffectResources.h"

// Tests inject their own samples; no Qt resource access, filesystem audio,
// sound hardware, credentials or remote endpoints are required.
afv_native::afv::EffectResources::EffectResources() = default;
